# Modelos de origem

Esta pasta pode ser usada para guardar localmente modelos VRM de origem ou cópias de segurança.

Arquivos `.vrm` são ignorados pelo Git para impedir que modelos sem permissão de redistribuição sejam publicados por engano. O modelo usado pela aplicação deve ser copiado para `web/models/AvatarSample_M.vrm`.
